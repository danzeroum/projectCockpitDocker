# Backlog de requisitos, cada item ligado a uma capacidade (CAP-*). Dá ao agente o QUE fazer e por
# quê, sem inventar escopo. Um item pode citar o risco que introduz e depender de outro.
# Schema: harness/schemas/backlog.schema.json
schema_version: "1.0"
metadata_version: "1.0"
source_of_truth: true
generated_from: null

# VAZIO DESDE A FATIA-4 DA CP-003. Os nove requisitos (REQ-001..009) apontavam todos para as
# quatro capacidades aposentadas: cinco para a harness de adoção, que virou repositório próprio,
# e quatro (REQ-005/006/007/009) para CAP-ALVO — a campanha de homologação.
#
# Estes quatro NÃO foram cumpridos nem cancelados: são a CP-001, que continua aberta, e voltam
# como requisitos do alvo quando aquela campanha for reaberta sobre a bancada de STAGE-DEPLOY.
# Mantê-los aqui apontando para uma capacidade que não existe mais seria backlog que descreve
# um repositório que não é este.
items: []
