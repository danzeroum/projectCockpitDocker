# Parecer de proporcionalidade — não RIPD. A escolha é consequência, não preferência:
# governance/data-inventory.yaml inventaria ZERO campos, e o julgamento exigido só sobe para RIPD
# completo quando o primeiro campo entra. Emitir RIPD sobre um sistema sem campo inventariado seria
# cerimônia com aparência de rigor.
#
# Schema: ../harness/schemas/privacy-review.schema.json
# Recalcular o fingerprint: python ci/audit_lgpd.py --print-fingerprint
schema_version: "1.0"
metadata_version: "1.0"
source_of_truth: true
generated_from: null

review:
  kind: parecer_proporcionalidade
  document: governance/parecer-proporcionalidade.md
  produced_by: agent
  skill: revisao-lgpd
  # REGRAVADO na fatia-4 da CP-003 (2026-08-06), e o motivo importa mais que o número.
  #
  # O escopo mudou porque o metadado governável mudou — saíram as declarações da harness extraída,
  # entrou STAGE-DEPLOY. Nenhum CAMPO com forma de dado pessoal foi criado, removido ou movido:
  # `governance/data-inventory.yaml` continua inventariando zero, o alvo continua em d6ae70ea, e os
  # riscos abaixo continuam sendo sobre o mesmo endpoint do cockpit, no mesmo arquivo, na mesma
  # linha. O julgamento não foi refeito porque não havia julgamento novo a fazer — o que havia era
  # um fingerprint que amarra o parecer a um estado do repositório, e esse estado mudou.
  #
  # Se um dia um campo entrar, isto deixa de valer: o tipo de julgamento exigido sobe de Parecer
  # para RIPD completo, e regravar o número sem rodar /revisao-lgpd passa a ser exatamente o
  # carimbo que este campo existe para impedir.
  scope_fingerprint: "sha256:18c352402a6032cfecb2b58825259b51feb400cba71de4cef045c9c3a09422bc"

  issues:
    - id: RISCO-001
      severity: P1
      location: workspace/target/app/routers/containers.py:96
      category: exposicao_incidental_em_log
      lgpd_article: "Art. 6º, III (necessidade)"
      pbd_principle: Privacy by Default
      status: open
      risk: RISK-ALVO-001
      summary: >
        `GET /api/containers/{id}/logs?tail=500` devolve a saída bruta de qualquer container do
        host. O cockpit não decide o que a aplicação de terceiro escreve no stdout — então dado
        pessoal chega aqui sem ter sido coletado por ninguém. É incidental por natureza e não por
        configuração, o que significa que nenhum ajuste do cockpit o elimina: só a redução do que
        se expõe (janela, autorização, retenção) muda o quadro.
        AS TRÊS ALAVANCAS, MEDIDAS em d6ae70ea (2026-08-06) — o texto acima as tratava como três
        pendências iguais, e não são:
        RETENÇÃO já está resolvida e o risco não sabia: `db.py:1043` declara
        `RETENTION_LOGS_DAYS = _env_int("RETENTION_LOGS_DAYS", 7, 1)` e `purge_logs` apaga de
        `logs_fts` o que passa de sete dias, com piso que impede o `=0` de um .env mal editado.
        JANELA é onde há obra, e ela é uma linha: `containers.py::container_logs` tem
        `tail: int = 500` e teto `min(tail, 5000)` — o pior caso por requisição é dez vezes o
        pedido comum. A proposta mínima está declarada em REQ-028.
        AUTORIZAÇÃO é a redução mais forte e a única que quebra invariante do produto ("leitura é
        livre, escrita é gateada"): fica listada com o custo dito, para decisão do dono, e NÃO vai
        como proposta.
        E há uma quarta que o texto não citava: MASCARAMENTO não se aplica a este caminho.
        `mask_inspect` roda na rota `/json`; `/logs` devolve `text/plain` sem passar por lugar
        nenhum — log é stdout arbitrário de terceiro, sem chave para casar, e mascarar por
        adivinhação produz falso negativo silencioso. É exatamente por isso que este risco não se
        fecha por mascaramento.
        CONTINUA `open`, e continua sendo o que impede `validate_all` de sair 0 neste repositório
        (`ci/audit_lgpd.py:490` reprova P0/P1 aberto). Fechá-lo exige obra no alvo.
      consumed_by:
        kind: risk
        ref: RISK-ALVO-001

    - id: RISCO-002
      severity: P2
      location: workspace/target/app/masking.py
      category: mascaramento_incompleto
      lgpd_article: "Art. 46 (segurança)"
      pbd_principle: Segurança Ponta-a-Ponta
      status: open
      risk: RISK-SEGREDO-001
      summary: >
        A máscara é por NOME de chave, com sete termos
        (`pass|secret|token|credential|apikey|_key$|private`) mais credencial embutida em URI.
        Chaves como `authorization`, `pwd`, `jwt` ou `cookie` atravessam intactas — e o modo de
        falha é o pior tipo, porque a saída VEM mascarada em outros campos e parece ter funcionado.
      consumed_by:
        kind: risk
        ref: RISK-SEGREDO-001

  # O GATILHO. Está aqui e não em prosa solta porque é a única parte deste parecer que muda o
  # regime inteiro: `incidental` -> `operator` promove o julgamento a RIPD completo, exige
  # encarregado (Art. 41) e os quatro endpoints do Art. 18. A condição é SISTEMÁTICO, não presente:
  # um e-mail num stack trace é incidente; um pipeline que despeja registros de titulares no stdout
  # de um container observado pelo cockpit é tratamento, e aí o papel mudou de fato.
  #
  # Quem observa: a varredura do léxico de ci/audit_lgpd.py sobre o alvo materializado, e qualquer
  # achado da régua em modo passive quando houver host de homologação (hoje bloqueado por
  # INCOMPLETE:target_url — ver THREAT-ALVO-SEM-HOMOLOGACAO).
  # Como sobe: change-proposal alterando project.yaml:classification.lgpd_relevance e
  # governance/data-inventory.yaml:controller.role no MESMO PR, com os campos inventariados.
  not_assessed:
    - >
      GATILHO DECLARADO — se a varredura encontrar dado pessoal SISTEMÁTICO nos logs expostos (e não
      incidental), `lgpd_relevance` sobe de `incidental` para `operator` por change-proposal, e o
      data-inventory ganha campos. Enquanto for incidental, `incidental` é a resposta verdadeira.
    - >
      O ALVO NÃO FOI AUDITADO EM REDE. Nenhuma requisição foi emitida contra o cockpit publicado:
      `danzeroum/docker` só expõe produção, e sem host de homologação os modos de rede são recusados
      com SCOPE_MISSING (docs/laudo-adocao.md). Este parecer é leitura de código, não medição de
      comportamento — e a diferença precisa ficar escrita, porque um parecer que não diz o que não
      olhou é indistinguível de um que olhou tudo.
    - >
      OS COMPONENTES DO COCKPIT AINDA NÃO EXISTEM como metadado (fatias 2..n da CP-003). Sem eles,
      `owning_component` não tem para onde apontar, e é por isso que `fields` está vazio em vez de
      preenchido com um CMP-* inventado.
