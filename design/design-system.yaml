# Identidade e metas do sistema de design DO ALVO auditado (o Docker Cockpit).
# Meta de acessibilidade é contrato, não aspiração: é a régua contra a qual os modos ux/frontend
# da suíte medem o cockpit publicado, e contra a qual o agente reviewer avalia mudanças de UI.
# Schema: harness/schemas/design-system.schema.json
schema_version: "1.0"
metadata_version: "1.0"
source_of_truth: true
generated_from: null

design_system:
  name: "Docker Cockpit — sistema de design do alvo"
  version: "0.1.0"
  accessibility_target: "WCAG-2.2-AA"
