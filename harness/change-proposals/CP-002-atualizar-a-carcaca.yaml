# Atualizar a CARCAÇA (schemas, fiscais, políticas) para a do molde, sem mudar o que os
# metadados DESCREVEM. A virada para `kind: derived` é decisão separada: CP-003.
#
# A medição que originou esta proposta (2026-08-05, molde em 6ce9309 vs. este repositório):
#
#   arquivos do molde ..... 186        compartilhados ..... 69, dos quais 30 idênticos
#   arquivos aqui ......... 107        só no molde ........ 117
#                                      só aqui ............ 38 (o nosso trabalho)
#
# Transplantando a carcaça estrita por cima de uma cópia deste repositório, SEM tocar em nenhum
# metadado, o fiscal novo aponta 45 achados. Agrupados por causa:
#
#   13  aperto de schema ....... project.kind obrigatório, classification virou enum fechado,
#                                assertions por ADR, exemptions em components, risk_exemptions,
#                                stages_manifest em harness.yaml
#    6  arquivos exigidos ...... target.lock, harness/stages.yaml, security/threat-model.yaml,
#                                security/dependencies.yaml, governance/data-inventory.yaml,
#                                governance/privacy-review.yaml
#   21  raízes declaradas ...... 12 regras + 4 REQ + 5 capacidades apontando para
#                                tests/integration/ e tests/e2e/, fora da raiz tests/unit/
#    5  CÓDIGO ÓRFÃO ........... escopo_regua.py, veredito.py e os dois testes dos PRs #7 e #8
#
# A última linha é a razão de a proposta existir e não ser só higiene: são cinco buracos de
# metadado abertos nesta semana, em código que já está no main, e o fiscal atual não os vê.
# Um repositório cujo fiscal não enxerga o código novo está verde por não estar olhando.
#
# ─────────────────────────────────────────────────────────────────────────────────────────────
# EMENDA (2026-08-05): esta proposta NÃO É EXECUTÁVEL como escrita. Medido ao tentar executá-la.
#
# Ela promete "não declarar kind: derived". Mas o project.schema.json novo torna `kind`
# OBRIGATÓRIO, com enum fechado de dois valores, e o target-lock.schema.json trava o par
# kind/target_sha nos dois sentidos. Não há terceiro estado — e o comentário do schema diz por
# quê: "não existe 'derivado quase pronto' — estado que, uma vez tolerado, dura para sempre".
#
# Então "CP-002 sem CP-003" só pode significar `kind: mold`. Executei assim, numa cópia:
#
#   45 achados -> 37. E os 37 param aí, porque 21 deles são INFECHÁVEIS nessa leitura:
#
#     ci/inventory_code.py:38   RAIZES_MOLDE = {"code": ["src"], "test": ["tests/unit"]}
#
#   É constante do ci/ do molde. Com kind: mold, a única raiz de teste é tests/unit/, e as
#   nossas 12 regras, 4 REQ e 5 capacidades apontam também para tests/integration/ e tests/e2e/.
#   Fechá-los exigiria editar o ci/ do molde — que é exatamente o fork que RISK-MOLDE-001
#   descreve — ou mudar o layout de testes deste repositório para caber num molde que ele não é.
#
# E `kind: mold` seria falso de qualquer forma. O molde define mold como "a casca genérica, que
# não governa alvo algum"; este repositório existe para governar danzeroum/docker, e é o que o
# próprio project.yaml:business.purpose declara. Declarar mold para não decidir a CP-003 seria
# mentir no campo mais estrutural do modelo novo, para adiar uma pergunta.
#
# SEGUNDO ERRO desta proposta, no parágrafo final do rationale: "primeiro fechar os 13 apertos de
# schema contra o fiscal atual, depois trocar os fiscais". Não dá. Os schemas ATUAIS têm
# additionalProperties: false — `assertions`, `due`, `exemptions` e `risk_exemptions` são
# REJEITADOS hoje. Eles só entram junto com a troca, nunca antes. O caminho aditivo que eu
# descrevi não existe.
#
# O que sobra de verdadeiro aqui: o modelo do molde não tem estado para "repositório com código e
# testes PRÓPRIOS que também governa um alvo", e é isso que este repositório é. A escolha real
# está na CP-003, e não há meio-caminho que a evite.
# ─────────────────────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────────────────────
# EMENDA 2 (2026-08-05, tarde): medição refeita contra a RELEASE v1.0.0 do molde, e não contra o
# `6ce9309` da emenda anterior. Os números mudam de tamanho e não mudam de conclusão.
#
# Transplante da carcaça v1.0.0 sobre uma cópia deste repositório, fiscais da v1.0.0 rodados:
#
#                          achados   reancoragem                                   carcaça
#   kind: derived .......    68      47  (24 regra + 8 REQ + 8 I5 + 6 I4 + 1 inv.)     21
#   kind: mold ..........    42      21  INFECHÁVEIS                                   21
#
# Os 21 infecháveis do `kind: mold` batem EXATAMENTE com o número que a emenda anterior previu
# medindo outro commit. Duas medições independentes, contra árvores diferentes, mesmo resultado:
# a leitura "CP-002 sem CP-003" continua sem saída, e agora está confirmada contra a versão
# publicada em vez de contra um commit de trabalho.
#
# O QUE FECHA A PORTA DE VEZ, e não estava na emenda anterior: `target-lock.schema.json` da
# v1.0.0 trava `mold_release` nos dois sentidos —
#
#     if kind == "derived"  then required: [mold_release]
#     else                       mold_release: {"type": "null"}
#
# Ou seja, ancorar este repositório numa versão do molde é ESTRUTURALMENTE IMPOSSÍVEL enquanto
# `kind` não for `derived`. O `/atualizar-carcaca` da v1.0.0 diz a mesma coisa na primeira linha:
# "Pare antes de começar se project.yaml:project.kind não for derived". Não existe atualizar a
# carcaça e adiar a CP-003: a âncora é a CP-003.
#
# STATUS: esta proposta permanece BLOQUEADA e é ABSORVIDA pela CP-003. O trabalho de carcaça que
# ela descreve (os 21 achados estruturais) vai junto com a reancoragem, porque nenhum dos dois
# atravessa sozinho.
# ─────────────────────────────────────────────────────────────────────────────────────────────
#
# Schema: harness/schemas/change-proposal.schema.json · Fiscal: ci/validate_metadata.py
schema_version: "1.0"
metadata_version: "1.0"
source_of_truth: true
generated_from: null

proposal:
  id: CP-002
  title: "ABSORVIDA pela CP-003 — atualizar a carcaça sem ancorar não é possível (ver emendas)"
  author_kind: agent
  created_at: "2026-08-05T00:00:00Z"
  # As quatro, e não por inflação: os schemas que validam capacidade, regra, requisito e
  # componente mudam todos, e a raiz de teste declarada muda para todas.
  capabilities_affected:
    - CAP-CONTRATO
    - CAP-MODOS
    - CAP-PROCEDENCIA
    - CAP-ALVO
  components_affected:
    - CMP-CONTRATO
    - CMP-PLANO
    - CMP-PROCEDENCIA
    - CMP-CLI
  risk_assessment:
    level: high
    risks:
      - RISK-MOLDE-001
      - RISK-META-001
      - RISK-CHANGE-001
    rationale: >
      Alto porque troca o FISCAL, e trocar quem mede é diferente de mudar o que é medido: um
      erro aqui não aparece como erro, aparece como verde. O fiscal novo é mais rigoroso em
      seis pontos e mais permissivo em nenhum — mas a comparação foi feita por transplante numa
      cópia, não por leitura, e um transplante que passa não prova que o repositório de verdade
      passa com o histórico junto.

      O que a proposta NÃO faz, e é o que a mantém neste nível de risco em vez de acima dele:
      não declara `kind: derived`, não cria `target.lock` e não muda a raiz do inventário. Os
      metadados continuam descrevendo o código DESTE repositório. A virada de significado — em
      que o derivado passa a descrever o alvo materializado em workspace/ e a nossa própria CLI
      sai do campo de visão do fiscal — é a CP-003, e pode ser recusada sem desfazer esta.

      Duas colisões conhecidas, com custo medido. Os ADR-005, 006 e 007 do molde são decisões
      inteiramente outras (catalogo-via-porta, conformidade-como-assercao,
      lgpd-sempre-em-dois-niveis) e colidem em número com os nossos: renumerar para 015-017
      alcança 35 referências em 17 arquivos. E `security/` e `governance/*.yaml` chegam do molde
      com dados de exemplo (CMP-CATALOG, CMP-PRICING) — precisam ser AUTORADOS para o cockpit,
      não copiados; copiá-los produziu 9 achados fantasma na medição, e um threat-model de
      mentira é pior que nenhum.

      O caminho é aditivo e reversível por partes: primeiro fechar os 5 órfãos e os 13 apertos
      de schema contra o fiscal atual (mudança de metadado que vale por si), depois trocar os
      fiscais. Nesta ordem, o CI reprova cedo se a declaração e o repositório discordarem.
  required_gates:
    - inventory
    - unit-tests
    - metadata-validation
  tests_required:
    - tests/integration/test_fiscal.py
    - tests/integration/test_repositorio.py
    - tests/integration/test_workflows.py
    - tests/e2e/test_checklist_adocao.py
  human_approval_required: true
  change_mode: pull_request
