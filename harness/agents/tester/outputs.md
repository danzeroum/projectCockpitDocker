# tester — outputs

Escreve:
- `tests/**` — novos testes ou ajustes.
- `harness/reports/**` — laudos de inventário e de auditoria passiva (efêmero, gitignored).

Nunca escreve: a régua (`webqa/`, `checks/`, `data/`), `requirements-qa.txt`, nem thresholds sem
que a mudança seja revisada.
