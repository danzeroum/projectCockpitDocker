# reviewer — outputs

Escreve:
- comentários de revisão (no PR ou em `harness/reports/review-*.md`).
- o inventário resultante em `harness/reports/` (efêmero, gitignored).

Nunca escreve: código de negócio (`src/`), a régua, nem o pin de dependência.
