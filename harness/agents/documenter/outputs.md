# documenter — outputs

Escreve:
- `docs/**`, `README.md` — documentação do negócio.
- resumos legíveis de laudos em `harness/reports/**` (efêmero, gitignored).

Nunca escreve: código de negócio (`src/`), a régua, `requirements-qa.txt`, nem `tests/qa/**`.
