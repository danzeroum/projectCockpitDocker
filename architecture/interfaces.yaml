# Contratos de interface entre componentes. O fiscal cruza:
#  - provider e consumers existem em components.yaml;
#  - exposes ⊆ exposes do componente provedor;
#  - cada consumidor declara depends_on o provedor (coerência com o grafo de componentes).
# Schema: harness/schemas/interfaces.schema.json
schema_version: "1.0"
metadata_version: "1.0"
source_of_truth: true
generated_from: null

# VAZIA DESDE A FATIA-4 DA CP-003, e a vacuidade é uma afirmação, não uma pendência.
#
# As quatro interfaces que viviam aqui (IFC-CONTRATO, IFC-PLANO, IFC-PROCEDENCIA, IFC-CLI) eram
# a API interna de `src/cockpit_harness/` — `provider`, `consumers` e `exposes` todos apontando
# para módulos do mesmo pacote. Ele saiu para danzeroum/cockpit-harness levando os contratos que
# fazem sentido dentro dele; declará-los daqui seria este repositório afirmar coisas sobre o
# formato interno de uma dependência sua.
#
# O alvo ainda não tem interfaces declaradas: a fatia-2 autorou os componentes do cockpit e
# NÃO as fronteiras entre eles, que é trabalho de outra fatia. Lista vazia é o estado honesto —
# uma IFC inventada agora seria contrato afirmado sem leitura.
interfaces: []
